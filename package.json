{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter axyomis run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "start:server": "node server.js"
  },
  "private": true,
  "dependencies": {
    "express": "^4.18.2",
    "groq-sdk": "^0.4.0"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "serve": "^14.2.0",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
