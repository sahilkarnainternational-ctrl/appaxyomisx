services:
  - name: axyomis-x
    type: web
    plan: free
    env: static
    buildCommand: npm install --prefix Axyomis-X && npm run --prefix Axyomis-X build
    staticPublishPath: ./Axyomis-X/dist
