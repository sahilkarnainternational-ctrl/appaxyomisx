services:
  - type: web
    name: appaxyomisx
    env: node
    plan: free
    region: ohio
    buildCommand: |
      cd Axyomis-X
      npm install
      npm run build
    startCommand: |
      cd Axyomis-X
      npm start
